rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Temporäre offene Regeln für Demo/Development
    match /{document=**} {
      allow read, write: if true;
    }
  }
}